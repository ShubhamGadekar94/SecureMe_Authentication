# SecureMe_Authentication
This microservice has the authentication and authorization functionality. After successful authentication system allocates a JWT token for authorization for other microservices
